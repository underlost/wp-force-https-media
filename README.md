# wp-force-https-media 

Probably not for you. This is just a little WordPress plugin that rewrites all URLs in WordPress' WP-JSON endpoints that start with `http://` to `https://`, regardless of WordPress settings. 
